# ACCESSIBILITY SETTING IN LOR
# Click to Action

class Bot:

    def __init__(self, initializer, debug):

        while True:

            # all_stats = [my_cards_stats, enemy_cards_stats]
            # all_cards = my_cards, enemy_cards, clean_codes - has x and y values

            # BUG FIXING - DOES NOT UPDATE IN GAME

            print(str(initializer.all_stats))
            print(str(initializer.all_cards))
